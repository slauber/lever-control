const config = {
  // defaults to Saitek Pro Flight Quadrant
  vid: 1699,
  pid: 3117,

  nodered: {
    username: "",
    endpoint: "",
    password: "",
  },

  levers: {
    // left lever
    videolights: 0,
    // right lever
    volume: 2,
  },

  // switches are bit-encoded on one byte
  switches: {
    bytes: 3,
    fan: {
      on: 0,
      off: 1,
    },
  },
};

module.exports = config;
